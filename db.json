{
  let debugContext = {
    AccessClaims: [
      {
        sensitiveData: "NOT_STORED_IN_OKTA"
      }
    ]
  };
  let commandArray = [
      {
        "op": "add",
        "path": "/claims/sensitiveData",
        "value": "NOT_STORED_IN_OKTA"
      }          
    ]  
  // Add our Token commands the handlers response command object
  const commands = [
    {
      "type": "com.okta.tokens.access_token.patch",
      "value": commandArray
    }       
  ];

  debugContext = JSON.parse(JSON.stringify(debugContext));
 
  const responseBody = {
      "result": "SUCCESS",
      "commands": commands,
      "debugContext": debugContext
  }  
}
